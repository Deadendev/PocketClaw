package com.inspiredandroid.pocketclaw

/**
 * Application version surfaced to users (e.g. in the settings footer) and to remote services
 * via the HTTP `User-Agent` header.
 *
 * Kept in sync with `appVersion` in `gradle/libs.versions.toml` whenever the version is bumped.
 */
object Version {
    const val APP_VERSION = "2.6.2"
}
